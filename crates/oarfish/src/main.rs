//! `oarfish` - the daemon and its CLI.
//!
//! Wiring only: this binary composes the crates and owns no pipeline logic of
//! its own.

#![forbid(unsafe_code)]

fn main() {
    println!("oarfish: not implemented yet");
}
